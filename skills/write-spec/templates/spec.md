# Spec: <feature>

<!-- instruction: WHAT + WHY, не HOW. Жодних технологій/реалізації — це plan.md. -->

## 1. User stories
<!-- instruction: As a <роль> I want <ціль> so that <користь>. Ролі — з glossary/CONTEXT, не вигадані user/admin. -->

## 2. Acceptance criteria
<!-- instruction: нумеровані, тестовані, однозначні. ≥1 AC на кожну user story. Покрий типи: happy / error / authorization / domain-invariant / cross-context.
     FORBIDDEN у AC: HTTP-дієслова (GET/POST/PUT/DELETE/PATCH), URL-шляхи (/api/...), коди статусів (200/404/...), SQL-конструкції (SELECT/JOIN/...), назви фреймворків/бібліотек/таблиць. Усе це — у plan.md. AC = бізнес-спостережувана поведінка. -->

## 3. Out of scope
<!-- instruction: що свідомо НЕ робимо в цій фічі. -->

## 4. Edge cases
<!-- instruction: порожні/невалідні дані, права доступу, конкурентність, ліміти, помилки. -->

## 5. Open questions
<!-- instruction: припущення + питання з власником і дедлайном (не лише «TBD»). -->
